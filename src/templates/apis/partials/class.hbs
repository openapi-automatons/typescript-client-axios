{{> comment}}
export class {{title}} extends AbstractApi {
	readonly #config: {{title}}Config;

  /**
   * constructor
   * @param {Config} config
   */
	constructor(config: Config = {}) {
		super(config);
		this.#config = new {{title}}Config(config.security);
	}
	{{#each paths}}

	{{> apis/class/request }}
	{{/each}}
}
